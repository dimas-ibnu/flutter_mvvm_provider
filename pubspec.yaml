name: provider_mvvm
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0-378.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  logger: ^1.3.0
  provider: ^6.0.5

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
